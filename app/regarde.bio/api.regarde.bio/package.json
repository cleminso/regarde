{
  "name": "@regarde-dev/api.regarde.bio",
  "version": "0.1.0",
  "description": "Jazz.tool worker for nickname registration.",
  "main": "dist/index.js",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/cleminso/regarde.git",
    "directory": "app/regarde.bio/api.regarde.bio"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:schema": "pnpm --filter jazz-schemas build",
    "build": "pnpm clean && pnpm run build:schema && tsc",
    "start": "pnpm build && node dist/index.js",
    "debug": "pnpm build && node --inspect dist/index.js",
    "start:bun": "pnpm build:schema && bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "keywords": [
    "jazz",
    "worker"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/node-server": "catalog:",
    "@hono/swagger-ui": "catalog:",
    "@hono/zod-openapi": "catalog:",
    "@regarde-dev/jazz-schemas": "workspace:*",
    "@regarde-dev/sdk": "workspace:*",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "jazz-tools": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "nodemon": "^3.1.10",
    "typescript": "catalog:"
  }
}
