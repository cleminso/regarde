{
  "name": "@regarde-dev/admin",
  "version": "0.1.0",
  "description": "CLI tool for profile nickname registry admin operations",
  "main": "dist/cli.js",
  "type": "module",
  "bin": {
    "profile-admin": "dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cleminso/regarde.git",
    "directory": "packages/admin"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:schema": "pnpm --filter jazz-schemas build",
    "build": "pnpm clean && pnpm run build:schema && tsc",
    "dev": "pnpm build:schema && bun --watch src/cli.ts",
    "cli": "pnpm build:schema && bun src/cli.ts"
  },
  "keywords": [
    "jazz",
    "admin",
    "cli",
    "nickname-registry"
  ],
  "dependencies": {
    "@alcyone-labs/arg-parser": "^2.7.2",
    "@regarde-dev/jazz-schemas": "workspace:*",
    "@regarde-dev/sdk": "workspace:*",
    "dotenv": "^16.6.1",
    "jazz-tools": "catalog:",
    "ulidx": "catalog:",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
