/**
 * Global test setup for profile-worker testing
 */

import { beforeEach } from 'vitest';

// Global test configuration
beforeEach(() => {
  // Reset any global state if needed
  // Individual tests will set up their own mocks
});
