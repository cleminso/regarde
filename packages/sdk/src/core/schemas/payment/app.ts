// move out `App` from `paymentEvent.ts`
// but we have a circular depedency where both need each other. How to move them into two separate schema?
// or simply leave them together to make it simple and rename `paymentEvent` for another name?
