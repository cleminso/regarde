{
  "buildCommand": "cd ../.. && corepack prepare pnpm@latest --activate && pnpm install && pnpm --filter=\"@regarde-dev/shared-schemas\" build && pnpm --filter @regarde-dev/profile-app build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && corepack prepare pnpm@latest --activate && pnpm install",
  "redirects": [
    {
      "source": "/:nickname",
      "destination": "https://api.regarde.bio/:nickname",
      "has": [
        {
          "type": "header",
          "key": "user-agent",
          "value": ".*(bot|crawler|spider|facebookexternalhit|twitterbot|whatsapp|telegram|signal|discord|slack|preview|linkpreview).*"
        }
      ],
      "statusCode": 302
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
