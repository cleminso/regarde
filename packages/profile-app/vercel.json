{
  "buildCommand": "cd ../.. && corepack prepare pnpm@latest --activate && pnpm install && pnpm --filter=\"@onboarding.jazz/shared-schemas\" build && pnpm --filter @onboarding.jazz/profile-app build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && corepack prepare pnpm@latest --activate && pnpm install",
  "rewrites": [
    {
      "source": "/:nickname",
      "destination": "/api/:nickname",
      "has": [
        {
          "type": "header",
          "key": "user-agent",
          "value": ".*(bot|crawler|spider|facebookexternalhit|twitterbot).*"
        }
      ]
    }
  ],
  "routes": [
    {
      "src": "/(.+)",
      "dest": "/index.html"
    }
  ]
}