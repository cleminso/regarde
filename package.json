{
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.1",
    "@testing-library/user-event": "^14.6.1",
    "@vitest/ui": "catalog:",
    "happy-dom": "^20.0.11",
    "jsdom": "^27.3.0",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@scure/bip39": "catalog:",
    "jazz-tools": "catalog:",
    "preact": "catalog:"
  },
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm --filter \"*.test.ts\" test:run",
    "cli": "tsx packages/cli/src/index.ts",
    "format-and-lint": "prettier . --check && eslint . --ext .ts,.tsx,.js,.jsx ",
    "format-and-lint:fix": "prettier . --write && eslint . --ext .ts,.tsx,.js,.jsx --fix"
  }
}
